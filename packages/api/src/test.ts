import fs from "fs";
import { parsePdfFromBuffer } from "@corpus/core";

const test = async () => {
  try {
    // find a pdf
    console.log("running test");
  } catch (e) {
    console.error(e);
  }
};
test();
